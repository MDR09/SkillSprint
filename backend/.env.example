# ========================================
# SkillSprint Backend Environment Configuration
# ========================================

# Application Settings
NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:5173

# ========================================
# MongoDB Configuration
# ========================================
# For MongoDB Atlas (recommended for production):
# MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/skillsprint?retryWrites=true&w=majority&appName=SkillSprint

# For Local MongoDB (good for development):
MONGODB_URI=mongodb://localhost:27017/skillsprint

# Comment out MONGODB_URI completely to run in mock mode without database

# ========================================
# Authentication & Security
# ========================================
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRE=7d

# ========================================
# OAuth Configuration (Optional)
# ========================================
# GitHub OAuth (Create app at: https://github.com/settings/applications/new)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Google OAuth (Create app at: https://console.developers.google.com/)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# ========================================
# Email Service Configuration (Optional)
# ========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# ========================================
# Performance & Security Settings
# ========================================
# Code Execution
DOCKER_TIMEOUT=30000
MAX_CODE_SIZE=1048576

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100

# ========================================
# Setup Instructions
# ========================================
# 1. MongoDB Atlas Setup:
#    - Go to https://cloud.mongodb.com/
#    - Create free cluster
#    - Create database user with readWrite permissions
#    - Add your IP to Network Access (or use 0.0.0.0/0 for development)
#    - Get connection string and replace username/password
#
# 2. Local MongoDB Setup:
#    - Install MongoDB Community Server
#    - Start MongoDB service
#    - Use: MONGODB_URI=mongodb://localhost:27017/skillsprint
#
# 3. Mock Mode (No Database):
#    - Comment out or remove MONGODB_URI line
#    - Server will use sample data for testing
